/** @type {import('next').NextConfig} */
const nextConfig = {
  // async rewrites() {
  //   return [
  //     {
  //       source: "/api/:path*", // Matches requests starting with `/api`
  //       destination: "http://localhost:8080/api/:path*", // Redirect to Go backend
  //     },
  //   ];
  // },
};

export default nextConfig;
